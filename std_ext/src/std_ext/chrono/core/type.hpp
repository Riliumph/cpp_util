#ifndef INCLUDED_STD_EXT_CHRONO_CORE_TYPE_HPP
#define INCLUDED_STD_EXT_CHRONO_CORE_TYPE_HPP
// STL
#include <chrono>

namespace std_ext {
namespace chrono {
using nsecs = std::chrono::nanoseconds;
using usecs = std::chrono::microseconds;
using msecs = std::chrono::milliseconds;
using secs = std::chrono::seconds;
using mins = std::chrono::minutes;
using hours = std::chrono::hours;
} // namespace chrono
} // namespace std_ext
#endif // INCLUDED_STD_EXT_CHRONO_CORE_TYPE_HPP
