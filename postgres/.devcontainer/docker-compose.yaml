version: "3"
services:
  dev:
    init: true
    build:
      context: ../
      dockerfile: .devcontainer/dev.dockerfile
      args:
        HADOLINT_URL: ${HADOLINT_URL}
    hostname: builder
    volumes:
      - type: bind
        source: ../
        target: /workspace
    stdin_open: true
    tty: true
